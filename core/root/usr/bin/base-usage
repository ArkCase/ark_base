#!/usr/bin/bash
exec cat <<EOF
This image serves as the base image for all ArkCase builder images.

It provides all essential libraries and development tools needed to
successfully build and run an application, as well as a pre-hardened
runtime environment as per STIG rules + guidelines.

By default it displays this message
EOF
